//#region Global Imports
//#endregion Global Imports

//#region Local Imports
import { {{upperFileName}}Helper } from '@ServiceHelpers';
import { BrokerHelper } from '@Test/Utils';
//#endregion Local Imports

//#region Interface Imports
import { } from '@Interfaces';
//#endregion Interface Imports

const broker = BrokerHelper.setupBroker();

describe('Test {{upperFileName}} service', () => {

	beforeAll(() => broker.start());
	afterAll(() => broker.stop());

	describe('Test {{upperFileName}} service actions', async () => {

		it('', async () => {
			const params = {
				// params
			};

			await {{upperFileName}}Helper.methodName(broker as any, params);
		});
	});

});
